server.port=8080

#DB properties

## quando avvi spring in locale
#spring.datasource.url=jdbc:postgresql://localhost:5432/cloud_demo

## quando avvi docker (le variabili vengono salvate su servizi AWS appositi e.g AWS Secret Manager)
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
